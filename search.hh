#ifndef SEARCH_HH_
#define SEARCH_HH_

#include <cassert>

int binarySearch(int arr[], int left, int right, int x);
int linear_search(int arr[], int size, int x);
int jumpSearch(int arr[], int size, int x);
#endif //ifndef SEARCH_HH_